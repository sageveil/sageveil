{
  "name": "@sageveil/site",
  "version": "0.2.0-rc.0",
  "private": true,
  "dependencies": {
    "@sageveil/palette": "workspace:*"
  },
  "nx": {
    "targets": {
      "generate-ports": {
        "executor": "nx:run-commands",
        "options": {
          "command": "node tools/scripts/ports-manifest.mjs",
          "cwd": "{workspaceRoot}"
        }
      },
      "build": {
        "dependsOn": [
          "generate-ports"
        ],
        "inputs": [
          "production",
          "^production",
          {
            "env": "BASE_PATH"
          }
        ]
      },
      "serve": {
        "dependsOn": [
          "generate-ports"
        ]
      },
      "preview": {
        "dependsOn": [
          "generate-ports"
        ]
      }
    }
  }
}
